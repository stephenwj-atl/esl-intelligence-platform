REPLIT PROMPT — CLOSE HIGH-PRIORITY VNEXT GAPS
Profile Comparison Engine + Multi-Instrument Portfolio Metrics + Funder-Specific Logic + Final Methodology Hardening

You are working inside the existing ESL Intelligence Platform codebase.

Do not return a plan. Do not summarize. Implement.

This is not a greenfield build. The platform already contains:

layered PERS logic
sector family weighting profiles
instrument logic
methodology records
memo generation
outcomes
disbursement and transition structures
overrides
validation cases
portfolio views

Your task is to close the highest-priority gaps identified in the VNext gap analysis and make the platform materially stronger for committee-grade and DFI-grade use.

If something is unclear, make the most defensible assumption, document it in /docs/BUILD_ASSUMPTIONS.md, and continue.

1. MISSION

Implement the following four major upgrades now:

Profile Comparison Engine
Multi-Instrument Portfolio Metrics
Funder-Specific Framework Logic
Final Methodology Hardening
add missing private sector profile
enforce calibration guardrails
complete seed/demo coverage
improve assessment persistence where needed

Do not spend time reworking already-functional UI unless required to support these upgrades.

2. PROFILE COMPARISON ENGINE (HIGHEST PRIORITY)

This is the biggest missing methodology feature.

2.1 Required capability

For any selected project, the platform must be able to run the assessment under:

active/default profile
family-specific active profile
one or more selected alternative profiles

Then show a structured side-by-side comparison.

2.2 Required outputs

For each compared profile, compute and display:

methodology profile id
sector family
country context score
project exposure score
sector receptor sensitivity score
intervention delivery risk score
instrument structure risk score
outcome delivery risk score
confidence score
pers base score
pers final score
decision signal
capital mode recommendation
monitoring intensity
transition readiness
recommended controls
recommended conditions
2.3 Comparison deltas

Show:

score delta vs current active profile
decision change
capital mode change
monitoring change
top driver changes
layer contribution changes
2.4 Required API endpoints

Add endpoints such as:

POST /api/methodology/compare/project/:id
POST /api/methodology/compare/portfolio/:id
GET /api/methodology/profiles
GET /api/methodology/profiles/:id

Payload should allow:

selected profile ids
optional instrument override
optional funder override
optional what-if assumptions
2.5 Required UI

Add:

project-level profile comparison page or tab
portfolio-level profile comparison page or panel
ability to choose 2–4 profiles
table view + driver summary view
highlight when decision changes materially
2.6 Persistence

Store comparison runs in a table like:

profile_comparison_runs
selected profiles
project/portfolio target
comparison results json
generated at
generated by

This comparison engine must be reusable by memo generation.

3. MULTI-INSTRUMENT PORTFOLIO METRICS

The portfolio layer is still too debt-centric. Fix this now.

3.1 Loan portfolio metrics

Add or refine:

average PERS
median PERS
capital at risk
pricing adjustment distribution
proceed / condition / decline counts
monitoring burden
low-confidence exposure
top high-risk assets
3.2 Grant portfolio metrics

Add:

average outcome delivery risk
implementation readiness distribution
disbursement readiness distribution
monitoring load
beneficiary concentration
project count by grant decision signal
PROCEED
PROCEED_WITH_CONTROLS
RESEQUENCE
NARROW_SCOPE
DEFER_PENDING_BASELINE
DO_NOT_FUND
impact exposure by country
impact exposure by sector family
projects with weak theory of change
projects with missing milestone evidence
3.3 Blended portfolio metrics

Add:

concessionality distribution
grant component distribution
debt unlock pipeline
transition readiness distribution
pathway mix
GRANT_FIRST
BLENDED_NOW
LOAN_READY_AFTER_VALIDATION
STRUCTURE_NOT_YET_BANKABLE
projects blocked by weak transition logic
milestone bottlenecks
structure mix by sector family
3.4 Portfolio filters

Support filters by:

instrument
sector family
country
funder
monitoring intensity
decision signal
transition readiness
confidence band
3.5 Required API

Upgrade portfolio endpoints to return:

overall summary
loan metrics section
grant metrics section
blended metrics section
filtered metrics
portfolio comparison support if relevant
3.6 Required UI

Upgrade portfolio command center to:

switch by instrument
show instrument-specific KPI cards
support filters
show transition pipeline for blended
show outcome risk for grants
4. FUNDER-SPECIFIC FRAMEWORK LOGIC

Move beyond seeded framework names.

4.1 Required funders/frameworks

Support explicit internal logic for:

IDB
IDB Invest
CDB
World Bank
GCF
GEF
Adaptation Fund
EIB
Equator Principles
4.2 Required structure

Create structured funder/framework records containing:

framework id
display name
instrument relevance
safeguard emphasis
reporting style
baseline/readiness expectations
disbursement controls relevance
results framework emphasis
environmental/social permissibility cues
typical conditions language
category mapping approach
notes and limitations
4.3 Decision support integration

Funder selection must affect:

recommended safeguards package
recommended conditions
required evidence
recommended reporting cadence
monitoring emphasis
disbursement control emphasis
theory-of-change scrutiny level
transition readiness expectations where relevant
4.4 Important rule

Do not pretend to perfectly replicate each funder’s full real-world policy stack.

Instead implement:

structured internal operating logic
funder-aware recommendations
clear limitations notes
4.5 Required UI

Add or upgrade a Funder Logic page so a user can see:

what changes when funder X is selected
expected safeguards posture
reporting style emphasis
results framework emphasis
readiness expectations
key warning notes
4.6 Required API

Add or upgrade:

GET /api/funders
GET /api/funders/:id
POST /api/funders/apply-to-project/:id
POST /api/funders/compare
5. ADD MISSING PRIVATE SECTOR PROFILE

The spec called for a private-sector productive investment profile. Add it properly.

5.1 Required profile

Create and seed:

PERS_PRIVATE_SECTOR_V1

Include:

family = private_sector_productive_investment
supported instruments
rationale
limitations
evidence note
review note
base layer weights
subfactor relevance
5.2 Suggested base weights

Use this unless the current architecture requires a documented equivalent mapping:

{
  "country_context_weight": 0.33,
  "project_exposure_weight": 0.27,
  "sector_receptor_sensitivity_weight": 0.20,
  "intervention_delivery_risk_weight": 0.20
}
5.3 Suggested profile emphasis

Private-sector productive investment should emphasize:

asset viability and exposure
implementation feasibility
governance and permitting relevance
confidence / predictability
bankability / investability posture
6. ENFORCE CALIBRATION GUARDRAILS AT RUNTIME

The gap analysis correctly notes that guardrails are documented but not enforced.

Implement actual runtime enforcement.

6.1 Required guardrails

For profile updates or calibration actions:

no top-level base weight may shift by more than ±0.10 from seeded family default without a calibration note and status change
no top-level layer may go below 0.05 or above 0.50 without methodology review approval
top-level base weights must normalize to 1.00
subfactor relevance must remain between 0.00 and 1.00
subfactor change > 0.20 requires calibration note

5 material subfactor changes automatically sets calibration status to UNDER_REVIEW

confidence modifier must remain within bounded equivalent adjustment limits
6.2 Required storage

Track:

original seeded values
updated values
delta
change reason
reviewer
timestamp
review status

Add tables if needed, such as:

methodology_profile_changes
methodology_reviews
6.3 UI/API

Add:

update profile endpoint with validation
review status display
visible warnings when a profile is out of band
7. IMPROVE ASSESSMENT PERSISTENCE

The current gap analysis notes that many layer scores are computed on the fly and not fully persisted.

Tighten this.

7.1 Required persistence

For each completed project assessment, persist:

all major layer outputs
instrument outputs
funder-aware outputs where relevant
profile id used
confidence inputs used
recommended controls
recommended conditions
explanation payload
7.2 Use assessment records

Prefer a dedicated assessment-history table or structured assessment snapshot table rather than only decorating the project row.

Support:

multiple assessments over time
re-runs under different profiles
comparison reuse
audit trace
8. COMPLETE SEED / DEMO COVERAGE

Close the remaining demo coverage gaps.

8.1 Minimum seeded projects

Ensure at least 10 cross-family demo projects including:

infrastructure loan
ecosystem grant
governance grant
disaster recovery grant
blended agriculture project
blended resilience program
TA governance project
private-sector productive investment project
one project where default profile vs family profile gives different decision
one project with strong funder-specific recommendations
8.2 Seed comparisons

Pre-seed comparison runs that demonstrate:

infrastructure vs default profile
ecosystem vs default profile
governance vs default profile
private-sector vs default profile
8.3 Seed overrides

Ensure at least 5 meaningful overrides exist with:

reasons
reviewers
timestamps
whether later proved correct
8.4 Seed memo runs

Ensure at least 5 stored memo runs using actual data.

9. BLENDED OUTPUT LABEL HARDENING

The gap analysis notes that blended logic exists but does not use the spec labels consistently.

Fix this.

9.1 Required blended labels

Support and surface these explicitly:

GRANT_FIRST
BLENDED_NOW
LOAN_READY_AFTER_VALIDATION
STRUCTURE_NOT_YET_BANKABLE

Use them in:

project detail
portfolio blended metrics
memo generation
explainability
transition pathway summaries
10. DOCUMENTATION UPDATES

Create or update:

/docs/PROFILE_COMPARISON_ENGINE.md
/docs/PORTFOLIO_MULTI_INSTRUMENT_METRICS.md
/docs/FUNDER_SPECIFIC_LOGIC.md
/docs/PRIVATE_SECTOR_PROFILE.md
/docs/CALIBRATION_GUARDRAILS_ENFORCED.md
/docs/ASSESSMENT_PERSISTENCE_MODEL.md

Also update /docs/API.md accordingly.

Documentation must distinguish:

fully implemented
seeded/demo
provisional
future enhancement
11. ACCEPTANCE CRITERIA

The work is only complete when:

project-level profile comparison works and shows score/decision deltas
portfolio-level profile comparison works or is supported via API
portfolio command center has distinct loan, grant, and blended metrics
funder selection materially changes recommendations and controls
private-sector methodology profile exists and is active
calibration guardrails are enforced at runtime
assessment outputs are persisted in reusable assessment snapshots
10+ demo projects exist with full coverage
blended output labels use the explicit spec labels
documentation reflects the actual implementation
the app runs end-to-end in Replit
12. EXECUTION ORDER

Execute in this order:

inspect current methodology, portfolio, funder, and assessment persistence code
add missing schema/tables for comparison runs, methodology reviews, profile changes, assessment snapshots if needed
add private sector profile
implement runtime calibration guardrails
implement project-level profile comparison engine
implement portfolio-level comparison support
implement multi-instrument portfolio metrics
implement funder-specific logic enhancements
harden blended output labels
seed missing projects, comparisons, overrides, memos
update docs
run and fix until usable
13. FINAL INSTRUCTION

Build this now.

Do not stop at surface-level UI.
Do not leave methodology comparison implicit.
Do not leave portfolio intelligence debt-centric.
Do not leave funder logic as labels only.

At the end output:

completed items
files changed
commands added
new seeded projects/comparisons/memos
remaining gaps
assumptions made